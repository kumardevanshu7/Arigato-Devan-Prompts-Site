# ─── LIVE (Hostinger root public_html) ───
# Copy this into: not-mine/.htaccess
# Replace local RewriteBase line with this single line:

RewriteEngine On
RewriteBase /not-mine/

RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^([a-z0-9][a-z0-9-]*)/?$ prompt.php?slug=$1 [L,QSA,NC]
